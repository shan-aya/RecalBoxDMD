Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "RYTHME" / then / line 2 = "& MUSIQUE" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-actionrythm.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Tir au pistolet". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-actionshootwithgun.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Infiltration". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-actionstealth.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "AVENTURE" / then / line 2 = "GRAPHIQUE" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-adventuregraphics.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "FILM" / then / line 2 = "INTERACTIF" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-adventureinteractivemovie.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "AVENTURE" / then / line 2 = "TEXTUELLE" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-adventuretext.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "ROMAN" / then / line 2 = "VISUEL" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-adventurevisualnovels.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "JEU DE" / then / line 2 = "PLATEAU" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-board.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Jeu Occasionnel". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-casual.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "COMPILATION" / then / line 2 = "MULTI JEUX" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-compilation.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "Démo" / then / line 2 = "Scène" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-demoscene.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "Cartes" / then / line 2 = "numériques" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-digitalcard.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Éducatif". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-educative.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "Jeu de" / then / line 2 = "Soirée" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-party.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Flipper". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-pinball.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "RPG D'ACTION". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-rpgaction.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "RPG DE GROUPE" / then / line 2 = "EN TEMPS RÉEL" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-rpgfirstpersonpartybased.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "RPG" / then / line 2 = "TACTIQUE" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-rpgtactical.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "CONSTRUCTION" / then / line 2 = "ET GESTION" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-simulationbuildandmanagement.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "CHASSE" / then / line 2 = "ET PÊCHE" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-simulationfishandhunt.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "SIMULATION" / then / line 2 = "DE VIE" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-simulationlife.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "Simulation" / then / line 2 = "Science-Fiction" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-simulationscifi.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "SIMULATION" / then / line 2 = "DE VÉHICULE" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-simulationvehicle.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "Sport" / then / line 2 = "Compétitif" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-sportcompetitive.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Sport de Combat". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-sportfight.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Course". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-sportracing.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "SIMULATION" / then / line 2 = "SPORTIVE" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-sportsimulation.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Stratégie". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategy.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 3 lines, EXACTLY: line 1 = "Explorer Étendre" / then / line 2 = "Exploiter" / then / line 3 = "Exterminer" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategy4x.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "ARTILLERIE". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategyartillery.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "ARÈNE DE BATAILLE" / then / line 2 = "MULTIJOUEUR" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategymoba.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "STRATÉGIE" / then / line 2 = "TEMPS RÉEL" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategyrts.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "STRATÉGIE" / then / line 2 = "TOUR PAR TOUR" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategytbs.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "DÉFENSE" / then / line 2 = "DE TOUR" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategytowerdefense.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "STRATÉGIE" / then / line 2 = "DE GUERRE" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategywargame.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "CULTURE" / then / line 2 = "GÉNÉRALE" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-trivia.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====
