Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "ACCIÓN". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-action.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Lucha". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-actionfighting.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "DISPAROS EN PRIMERA" / then / line 2 = "PERSONA" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-actionfirstpersonshooter.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "PLATAFORMAS". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-actionplatformer.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "PLATAFORMAS" / then / line 2 = "DE DISPAROS" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-actionplatformshooter.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "RITMO" / then / line 2 = "Y MÚSICA" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-actionrythm.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Disparo con pistola". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-actionshootwithgun.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Sigilo". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-actionstealth.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "AVENTURA" / then / line 2 = "GRÁFICA" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-adventuregraphics.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "PELÍCULA" / then / line 2 = "INTERACTIVA" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-adventureinteractivemovie.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "TERROR DE" / then / line 2 = "SUPERVIVENCIA" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-adventuresurvivalhorror.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "AVENTURA" / then / line 2 = "TEXTUAL" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-adventuretext.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "NOVELA" / then / line 2 = "VISUAL" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-adventurevisualnovels.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "JUEGO DE" / then / line 2 = "MESA" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-board.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Juego Casual". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-casual.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "COMPILACIÓN" / then / line 2 = "MULTIJUEGO" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-compilation.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "Demo" / then / line 2 = "Escena" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-demoscene.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "Cartas" / then / line 2 = "digitales" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-digitalcard.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Educativo". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-educative.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "Juego de" / then / line 2 = "Fiesta" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-party.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "PUZLE Y" / then / line 2 = "LÓGICA" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-puzzleandlogic.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "RPG DE ACCIÓN". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-rpgaction.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "RPG DE GRUPO" / then / line 2 = "EN TIEMPO REAL" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-rpgfirstpersonpartybased.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "RPG" / then / line 2 = "TÁCTICO" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-rpgtactical.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Simulación". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-simulation.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "CONSTRUCCIÓN" / then / line 2 = "Y GESTIÓN" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-simulationbuildandmanagement.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "CAZA" / then / line 2 = "Y PESCA" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-simulationfishandhunt.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "SIMULACIÓN" / then / line 2 = "DE VIDA" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-simulationlife.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "SIMULACIÓN" / then / line 2 = "DE CIENCIA FICCIÓN" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-simulationscifi.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "SIMULACIÓN" / then / line 2 = "DE VEHÍCULOS" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-simulationvehicle.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "Deporte" / then / line 2 = "Competitivo" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-sportcompetitive.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Deporte de Combate". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-sportfight.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Carreras". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-sportracing.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "DEPORTE". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-sports.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "SIMULACIÓN" / then / line 2 = "DEPORTIVA" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-sportsimulation.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Estrategia". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategy.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 3 lines, EXACTLY: line 1 = "Explorar Expandir" / then / line 2 = "Explotar" / then / line 3 = "Exterminar" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategy4x.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "ARTILLERÍA". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategyartillery.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "ARENA DE BATALLA" / then / line 2 = "MULTIJUGADOR" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategymoba.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "ESTRATEGIA" / then / line 2 = "EN TIEMPO REAL" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategyrts.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "ESTRATEGIA" / then / line 2 = "POR TURNOS" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategytbs.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "DEFENSA" / then / line 2 = "DE TORRE" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategytowerdefense.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "ESTRATEGIA" / then / line 2 = "BÉLICA" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-strategywargame.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "Cultura" / then / line 2 = "General" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-trivia.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "AVENTURA". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-adventure.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "AVENTURA 3D" / then / line 2 = "EN TIEMPO REAL" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `genre-adventurerealtime3d.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "Todos Los Juegos". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `allgames.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "FAVORITOS". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `favorites.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on 2 lines, EXACTLY: line 1 = "ÚLTIMOS" / then / line 2 = "JUGADOS" -- use exactly these pre-split parts, do not invent a different split, and never repeat the same word on more than one line. Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `lastplayed.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====

Here is a crop of ONLY the text portion of a retro pixel-art video game genre logo (no character/icon in this image -- do not generate one). Reproduce this text logo EXACTLY, replacing only the letters with new text, as if the same artist who made this exact image had written different words with the exact same materials and tools. Match with maximum precision: the exact font family/letterform style and weight, the exact colors and gradient direction, and above all the exact SURFACE TEXTURE/GRAIN of the letters -- pixel-grain for pixel-grain (rough, scratched, weathered, noisy, or whatever grain is visible), not a smooth clean flat fill. Match the exact black outline and drop shadow style. Match the exact text baseline and composition -- if the text curves along an arc, the new text must curve along that exact same arc. The new text replaces the ENTIRE original text -- the text must be on a single line: "PORTADOS". Match the same number of lines as the reference and the same per-line size/color scheme if there is more than one line (do not merge or unify colors across lines). Do not add, enlarge or invent any new effect, spark, particle or embellishment that isn't already visible. Do not add any grain/noise/filter beyond what is already on these letters. Use the EXACT SAME capitalization style shown here (ALL CAPS stays ALL CAPS, Mixed/Title Case stays as shown). Pure black background, same aspect ratio as this crop. Output a single image, not a grid or collage. Name the output file `ports.png` if your tool lets you choose a filename, otherwise state that exact filename in your reply.

=====
